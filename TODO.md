# TODO / состояние разработки

## Сделано

- C++20/CMake приложение на Dear ImGui, SQLite, GLFW/OpenGL.
- Открытие/создание баз через ImGuiFileDialog.
- История последних баз в меню `Файл`.
- Автооткрытие последней базы.
- Стартовое окно: только `Дашборд`.
- Верхнее меню:
  - `Файл`;
  - `Работа`;
  - `Справочники`;
  - `Прочее`.
- Отдельные окна справочников:
  - физлица;
  - сотрудники;
  - должности;
  - виды активности;
  - периоды активности;
  - нормы времени;
  - учреждение.
- Справочник сотрудников:
  - физлицо;
  - должность из справочника;
  - норма времени;
  - подразделение;
  - табельный номер;
  - дата приема;
  - дата увольнения;
  - активность.
- Нормы времени:
  - ставка;
  - часов в день;
  - часов в неделю;
  - дней в неделю;
  - короткая пятница на 1 час.
- Периоды активности для отпусков, больничных и прочих отклонений.
- Автозаполнение табеля:
  - по норме времени сотрудника;
  - с учетом короткой пятницы;
  - с перекрытием периодами активности;
  - с учетом дат приема/увольнения в модели заполнения.
- Форма `Табели` объединяет список табелей и табельную сетку.
- Убраны лишние дублирующие кнопки из формы `Табели`.
- Добавлен кастомный виджет даты с вводом `YYYY-MM-DD`, автоформатированием из 8 цифр и popup-календарем.
- Исправлено сохранение дат `Прием на работу` и `Увольнение` в карточке сотрудника.
- Исправлено переключение месяца/года в popup выбора даты.
- Проект пересобран после правок.
- Проверен сценарий новой базы через модель данных:
  - создание справочников;
  - добавление сотрудника с датами приема/увольнения;
  - создание и автозаполнение табеля;
  - отсутствие ячеек до приема и после увольнения;
  - перекрытие периода активности;
  - короткая пятница по норме времени.
- Добавлена печатная форма табеля 0504421 через HTML:
  - генерация файла во временную папку;
  - автоматическое открытие через системный обработчик;
  - печатный CSS для A4 landscape.
- Добавлен экспорт табеля в ODS:
  - используется редактируемый шаблон `assets/0504421.ots`;
  - добавлены правила маркеров `assets/0504421_TEMPLATE_RULES.md`;
  - шаблонный `content.xml` заполняется текущими сохраненными данными табеля;
  - служебные файлы и manifest шаблона сохраняются корректно для обычного `.ods`;
  - результат автоматически открывается системной программой по умолчанию.
- При сборке runtime assets копируются в директорию рядом с бинарником:
  - шрифты;
  - шаблон OTS;
  - правила разметки шаблона.

## Дальше

- Ручная GUI-проверка того же сценария в окне приложения.
- Доработка маркеров и внешнего вида `assets/0504421.ots` после ручной правки шаблона.
